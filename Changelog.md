# 0.9.7

## 历史消息正则处理

- 新增按楼层独立执行的历史消息正则流水线，规则支持「提取」与「排除」。
- 支持 `/pattern/flags` 与纯正则写法；`g` 等 JavaScript flags 原样兼容。
- 提取规则存在捕获组时取第一个有定义的捕获组，否则取完整匹配。
- 每条楼层先按规则从上到下处理，全部楼层处理后才交给后续 Tracker 流程；不修改宿主原始聊天数据。
- 设置页增加规则排序、启用/禁用、删除和当前楼层预览。
- 预览默认使用送入分析的最近 `contextSize` 楼，也可指定 1-based 起止楼层。

# Changelog

## 未发布

- 修正手动重复分析当前楼层时的 BS 状态基线：当前楼层已有插件快照时，改用该楼之前最近的 BS 快照，避免重复叠加工具调用；当前楼层尚未保存插件快照时，也只沿用最近的 BS 插件记录，不读取酒馆自身数据作为基线。
- 修正完整变量面板手动把角色阶段改为排卵期时未同步自然受精结算状态的问题：现在会重置 `naturalConceptionResolved`、`naturalOvulationUsed`，并从当前有效残留精液补建 `conceptionCandidates`；单独修改 `eggs` 不会重置周期。

## v0.9.7

### 新增與調整

- 「櫻花」主題改造成貼水鑽的貝殼機，英文副標從 Modern 改為 Clamshell。鉸鏈接縫壓在螢幕與按鍵列的現成分界上、上下殼兩種明度、兩側加鉸鏈圓柱；水鑽貼在螢幕邊框而非機身周邊（四角的 50px 圓角會切斷直排的鑽），大鑽排一圈、小鑽錯開半格。造型只做暗示不真的切成上下半——切了內容區會從 351px 剩約 160px。
  - 此主題的螢幕改為 `border-box`，順帶修掉螢幕比容器高 24px 而壓住按鍵列的既有錯位，代價是內容區 351px→333px。其餘十二套維持原狀。
- 新增第 13 套主題「果機 (iPhone)」：iOS 風格的淺／深色介面、系統字體、藍色強調色，機身走鈦金屬＋玻璃背板（五層材質全部用 `color-mix` 從機身色推導），螢幕為黑色細邊框＋大圓角並關掉共用的 CRT 掃描線，頂端品牌字改畫成靈動島。
- 「果機」附帶調色盤與字體自訂（選中該主題時才出現）：淺／深底色、六個強調色＋自訂、六種機身色＋自訂（機身不隨日夜改變），以及系統／圓體／襯線／等寬／手寫五組字體堆疊。只作用於果機，切回其他主題時自訂值會被清掉；字體只提供整組堆疊而不讓使用者直填 `font-family`——中文字型缺 fallback 會整片缺字。
- 註冊懷孕角色時，AI 可以直接設定特殊來歷的胎兒（同卵／異期複孕／孕中孕／胎內回歸；代孕走既有的 `provider`）。此前這些欄位會被註冊白名單靜默丟棄，開場設不了。
  - 模型直接寫 `tags` 與支撐欄位，矛盾的組合會在註冊後逐項撤銷：落單的同卵、指不到宿主或指向自己的孕中孕，受精點則夾進這次妊娠的範圍。**寧可少一個標籤，也不要留一個指向虛空的關係。**
  - `identical` 都標上就自動歸組；`nested` 用陣列下標指宿主，註冊後換成 `embryoId`；`revealed` 省略時按孕齡自動判定，想讓角色暫時不知情就給 `false`。
- 特殊來歷的胎兒出現時，寫故事的主模型也會收到來歷說明——此前只有追蹤器那條支流拿得到解釋，主線只收到一串原始 `tags`。與種族短敘述同規則，只解釋本輪出現過的標籤。標籤說明一律改寫成「怎麼造成的」，並修正 `nested` 講反了方向（原本描述的是宿主，但標籤打在長在別人體內的那一顆上）。
- 新增孕中孕（胎中胎）：異期受精的那一顆落進另一顆胎兒體內。三個硬條件同時成立才觸發，不另加機率係數——視窗 8–12 週（有效孕日 56–84）、宿主胎重 ≥ 1.5、子宮內精液總量 > 100。
  - 宿主取最重的一胎、同重時優先女胎，待著床的胚胎不能當宿主。母方是宿主胎兒、父方是精源；血緣圖上它的母親是同胎的另一個孩子，承載者另列為「承載」。
  - 要到孕晚期（有效孕日 189）才對模型與追蹤頁顯示，比一般異期胎晚一整個孕中期，期間照常發育與消耗供養力。宿主被減胎時一起消失。
- 新增異期複孕：已在妊娠中的角色可以再次受精，兩胎孕齡不同步，最後一起生下來。
  - 走既有的高潮排卵，只在孕早期打開受精閘門，機率為正常的 10% 並隨進度線性衰減至零。視窗上限是 84 減去著床所需時間換算的有效日（人類約 0–78），否則末尾受精的胚胎會來不及著床就撞上孕中期的強制清除。
  - 每胎孕齡改用偏移記錄（`conceivedAtDays`），階段仍由最早那胎驅動。起始胎重按落後進度打折 `(1 − 落後有效日 / 280)²`，乘在既有的種族混血偏移之上。
  - 進入孕中期之前，提示詞投影與追蹤頁都看不到它、`fetusesCount` 也不計入，但它照常發育與消耗供養力——揭曉前模型會看到「負擔比胎數應有的更重」。完整變量頁不受影響，分娩時強制揭曉。
  - 著床失敗只掉自己，先來那胎不受影響；沒能在孕中期前著床則一律清除，不會活到分娩變成孩子。同卵分裂與嵌合融合只作用於本批新胚胎。假孕期與回歸期不會被插隊。
- 新增 `bsWombReturn`（胎內回歸）：讓一個人回到另一名角色子宮內成為胎兒，過渡後轉入正常妊娠，出生時是全新個體（新的 child id）。
  - 回歸者不必是已註冊角色——常態是 user 被吞、或吞一個路人。未註冊時用 `returnerRace` 指定種族，未註冊者沒有凍結也沒有天賦可繼承。
  - 只能在承載者處於月經階段或無經期時呼叫，子宮內既有的胎兒、殘留精液與卵子會被淨空。新增過渡階段「回歸期」：衣著壓力頂到上限 10、該胎胎重為上限 3.0，隨剩餘時間線性回落，時間到自動轉入孕早期第一天；`hours` 傳 0 為瞬間完成，超出的時間會帶進妊娠。
  - 該胎母方為承載者、父方為回歸者，帶 `rebirth` 標籤，回歸者的天賦傳給這一胎（技能不傳）。已註冊的回歸者整個凍結：離場且停止一切階段推進，用 `bsSetCharacterPresence` 設回在場即可解凍。
  - 回歸期不受子宮壓力影響，不會自然流產；此期間呼叫 `bsAbortion` 代表回歸者被消化吸收、並入承載者體內而不是被排出。轉入孕早期後即為正常胎兒。
  - 不能回歸自己的子宮，回歸期中不能重複回歸或植入外源胚胎，`bsSetMenstrualPhases` 不能覆蓋回歸期。族譜把回歸者標為「前身」而非「父」——那個位置上站的往往是女角色——但仍算遺傳親代。

### 修正

- **修正多胎妊娠會互相覆蓋身分的嚴重缺陷。** 孕晚期的斜位重新定位用收集當下的索引去 `splice`，每搬動一胎陣列就位移一次，實際發生的是刪掉別人再把自己插回去——一胎被消滅、另一胎複製兩份，父方／種族／性別／標籤／胎教天賦互相覆蓋，族譜也跟著錯。改為按物件身分移除。修正前 2 胎跑 150 次有 **127 次**父方重複、3 胎 **137 次**，修正後 2/3/4 胎皆為 0。此缺陷自孕晚期胎位機制存在以來一直都在，與異期複孕無關。
- 修正 `bsSetCharacterPresence.isPresent` 在 schema 上是選填、實作卻要求必填，模型照 schema 省略就會撞到拒絕。schema 改為必填。
- 補齊工具說明裡缺的規則：`bsNameChild.childIndex` 與 `bsAbortion.fetusIndex` **從 0 起算**（系統通知寫的「第 2 胎」是 1 起算，照抄會差一位）；`bsSetMenstrualPhases` 列出實際接受的六個階段並說明妊娠階段與回歸期一律拒絕；`bsChildbirth` 補上階段條件；`bsUpdateCharacterStatus` 的四個數值是變化量而非目標值；`bsPassedTime` 各單位可相加、只能往前推、推進的是所有角色。另新增說明與實作的一致性測試，避免日後改了行為忘了改說明。
- 工具參數裡的人名支援 ST 的 user 巨集（`user`／`{user}`／`{{user}}`／`<user>`），涵蓋 `female`、`male`、`provider`、`fathers`、`returner`。這些名字最後會成為血緣圖的節點 id，不解析的話族譜會冒出一個叫「{{user}}」的人。限制：只認完整別名而不跑 `substituteParams`；嵌合體的雙父源只按全形 `×` 拆（拉丁 `x` 會切壞 `Max` 這種名字）；拿不到宿主 context 時原樣保留。
- 胎內回歸的邏輯審計，補上六個漏洞：同一名回歸者被同時塞進兩個人體內、已是別人體內胎兒的角色又當承載者（唯一會真的卡死的組合）、承載者被註銷後回歸者永久凍結（現在會自動解凍）、回歸期中植入外源胚胎、`hours` 傳負數被靜默當成瞬間完成、凍結只擋時間推進而其他生理類工具照樣能改胎兒狀態。
- 回歸期的心理更新改走 `preg` 組。原本落在 `mens`，而它會在真妊娠滿 7 天時被清空，等於回歸期寫進去的心理資料註定被抹掉。
- 時間推進的階段機新增未知階段警告。未知階段本身不會卡死角色（會被 `syncCharacterStageFromProfile` 重設回月經階段），真正會卡住的是「階段已加進保留清單、卻忘了在推進機加分支」，警告是為了照出這一種。
- 族譜的卡片與詳情欄補上年齡，與追蹤頁概覽同一套算法——同一個角色在兩個畫面顯示不同歲數會被當成 bug。未註冊的路人沒有年齡資料，該行留空但保留高度。
- 修正孕期衣著壓力的告警配色在多數主題下幾乎看不見（量測 12 套主題，最差的 retro 只有 1.93——紅配綠是色相很跳但亮度幾乎相同的組合）：負值數字改用該主題本文色並加粗，缺口條加上本文色斜紋，靠紋路而非色相辨識。告警語義不變。

### 維護

- 全庫掃過死碼，移除 11 個無人呼叫的函式（108 行）：`index.js` 的 `getCharacterStateForDisplay`、`renderCardList`、`getWardrobeItemKind`、`getPromptDisplayMeta`，以及 `api.js`、`race_config.js`、`state.js`、`tools.js` 共 7 個。全部在全庫僅出現一次（就是自己的定義），行為無變化；`registry.js` 掃描結果為零。
- 移除 `scripts/state.js` 裡一整組從未被呼叫的 profile 清洗函式（`sanitizeProfilePatch` 及其專屬的七個輔助函式），共 342 行。這組是給「模型直接寫整包 profile」用的守門員，但工具面從來沒開過這種門——`git log -S` 顯示初始 commit 起就沒有任何呼叫者。真正在跑的是 `registry.js` 的 `sanitizeRegistryProfile`／`sanitizePregnant`，註冊流程才是唯一會吃模型自由生成 profile 的路徑。

## v0.9.6

### 新增與調整

- 新增 API 格式切換（系統設定頁）：除既有的 OpenAI 相容外，新增 OpenAI Responses、Claude Messages 與 Gemini Interactions 三種上游格式，端點與請求體由擴展按格式翻譯，設定頁會即時預覽實際請求的 URL。預設仍是 OpenAI 相容，既有設定完全不受影響。
  - 傳輸選擇：相容格式走宿主代理；非相容格式在 TauriTavern 交由後端按 `custom_api_format` 翻譯，在原版 SillyTavern 改走 `/proxy/` 透傳（需 config.yaml 開啟 `enableCorsProxy`），皆不可用時回退瀏覽器直連。
  - 各格式的鑑權頭分開處理：Gemini 用 `x-goog-api-key`，Claude 併送 `x-api-key` 與 `anthropic-version`。
  - 感謝 @shuiyue-cmyk 貢獻（PR #4）。
- 經宿主後端代理發出的上游請求改為帶擴展自己的 User-Agent。原版 SillyTavern 後端用 node-fetch，預設 UA 是泛用的 `node-fetch`，部分供應商拿 UA 做反欺詐信號會直接攔截。TauriTavern 自帶產品 UA 且順序在後，故略過不注入。感謝 @shuiyue-cmyk 貢獻（PR #6）。
- 新增胎兒標籤 `fetuses[*].tags`：標注這顆胎兒是怎麼來的（嵌合體／代孕／自交／同卵），介面上以小標籤顯示在胎兒卡頂端，沒有標籤時整列不出現。標籤在分娩時跟著孩子記錄一起留下。
  - 分兩類來源：能從既有欄位推導的（嵌合體、代孕、自交）不落盤，存量存檔不必遷移就能顯示，也不會出現「資料改了但標籤還留著舊的」；推導不出來的才寫進 `tags`。同卵分裂屬於後者——複製體與原胚在欄位上完全一樣，事後無從分辨，只能在分裂當下把整組打上標籤與共用的 `identicalGroup`。
  - 目錄已預留 `rebirth`／`superfetation`／`nested`／`androgenesis`／`gynogenesis`（胎內回歸／異期複孕／孕中孕／雄核發生／雌核發生），目前沒有任何流程會產生，等對應玩法實作時直接寫入 id 即可，不必再改資料結構。
  - 提示詞只解釋本輪真的出現過的標籤，與種族短敘述同規則，沒用到就不佔 token。
- 胎兒卡的 `provider` 一行改標「遺傳母方」（原本直接把欄位名印在介面上），並新增「嵌合來源」一行——嵌合資料此前完全沒有在介面上呈現過。
- 衣柜頁的「孕期衣著壓力」重畫：原本只是標題旁一個空心藥丸裸數字，看不出量表範圍，四維餘裕（gap）也完全沒顯示。改成四個維度（遮蔽／承托／餘裕／便利）各一條小量表，2x2 排列、共用同一條壓力刻線：長條是該維度的衣物總值，條子沒頂到刻線就把差額用紅色補在缺口上。四條刻線在同一個 x 位置，掃一眼就知道哪幾維壓不住，不必逐個讀數字自己減。紅色沿用既有的產程危急色，不另外引入新色。
- 追蹤頁的「精液來源」新增占比環：畫出各來源殘留量占總量的比例，兩個以上來源才顯示（只有一個來源時整圈都是他，看不出資訊）。環心標總殘留量，圖例列出對象、百分比與原始數值。
  - 占比正是引擎受精判定裡的 share 項——每個來源的命中率會乘上自己的占比。但占比不等於最終受孕機率：同族／異族、胎生卵生不同還會各自乘上難度係數，所以只標占比，不標機率。
  - 不引入新色盤：12 套主題配色差異太大，改用同一個 currentColor 的階梯透明度加分隔缺口區分扇區。
- 追蹤頁的「經歷」分頁不再平鋪孩子卡，改為一句「共 N 名子女」摘要加一個族譜按鈕；孩子的完整資料移進新的血緣視窗。
- 新增血緣視窗：獨立全屏覆蓋層，以當前角色為中心分代橫排（祖輩／父母輩／本人／子女）。同一對親代的手足聚成一叢，叢上方掛親代標注並以連接線接到每張卡；點選任一人即高亮其親代與子代，詳情欄展開資料。
  - 跨代的連線不預先畫出——分代橫排下每條邊都畫會糊成一團，改為選取時才標示關係。
  - 手機單欄（詳情在下方）、平板以上左右並排、桌機加寬節點。每一代各自橫向捲動，開窗時先把中心角色捲到視野中央。
  - 代孕的承載者不算遺傳親代，不進親代標注，改在詳情欄另列「孕育者／代孕承載」兩行；嵌合體只連首位父母，其餘來源在詳情欄註明；自交的「母·父」會合併成一筆，不重複列出同一人。近親繁殖會讓同一人同時落在兩個世代距離上，此處取最近的一條。
  - 未註冊的路人只作為親代節點出現，不可展開詳情。
  - 裁切深度預設上下各兩代。被深度截斷或未登記的上一代不會憑空補出來，該人的詳情欄改標「在本故事中出生（上代未顯示）」。
- 分娩時保留父系種族：孩子記錄新增 `fatherRace` 與 `fatherDerivedType`。胎兒本來就帶這兩個欄位，但分娩時只留了混血後的 `race`，父方的血統就此失傳——未註冊的父親在血緣圖上因此無從標示種族。
- 孩子記錄補上穩定標識 `id`：此前只能用「母親名 + children 陣列索引」引用，改名或搬移孩子都會讓引用失聯。出生時即產生，存量存檔在讀取時自動回填，一旦產生就不再變動。
- 補上精液殘留的時間語義：提示詞現在會告知模型殘留每天自動衰減 10、歸零即消失，不需要每輪重複描寫流出；`bsAddSperm` 的 amount 補上建議區間 10-30（即 1-3 天內自然清空），此前完全沒有標度說明，模型隨手給大數字就會讓正文連續多日描寫殘留。`bsDrainSperm` 補上後果說明：受精是在每次時間推進時用當下仍存在的精液判定，清空後這次性交不再有受孕機會，單純洗澡沐浴不該呼叫本工具。行為未變，只是把既有規則講清楚。

## v0.9.5

### 新增與調整

- 新增「MVU 額外模型解析相容」設定（系統設定頁，預設開啟）：MVU 變量框架以「額外模型解析」更新變量時，自動輪詢的追蹤請求會等該輪變量更新結束後才發送，避免與 MVU 的更新請求重複消耗額度，也避免追蹤請求因 MVU 事後改寫消息而作廢重發。
  - 未偵測到 MVU 信號的角色卡零延遲；MVU 未開啟自動請求、`after_user` 觸發時機、使用者手動分析皆不受影響。
  - 等待有上限：未等到信號出現時最多寬限 4 秒，單輪等待上限 2 分鐘，逾時照常送出追蹤。每個聊天第一次進入等待時提示一次。
  - 主流程（mainflow）世界書模式下，快照捕獲會跳過 MVU 的額外解析請求，避免追蹤上下文被 MVU 的更新提示詞取代。
- 追蹤進行中會顯示常駐提示，結束時換成結果提示（有無狀態變化分開顯示）。原本只有浮球在追蹤**結束且有變化**時閃一下，沒有變化或還在跑時完全沒有可見反饋，手機上尤其難判斷。手動分析與自動追蹤都會顯示。
- 為 59 個內建種族與 10 個衍生類型補上「物種／衍生短敘述」，各附英文原名（如 兽人／Orc、妖精／Fairy），在該項目登場時併入提示詞，供模型判斷形象歸類（例如鯨魚娘該歸入空鯨或魚人、狐娘該寫兽耳族-狐還是妖狐）。衍生類型此前完全沒有內建短敘述。
  - 短敘述沿用「使用者覆寫優先」的既有規則：在種族百科改寫後，生理設定區塊與種族名錄會同步跟著改（例如把精靈改寫成「本世界的精靈全為扶她」）。
- 新增「提示詞附帶種族名錄」設定（預設開啟）：把系統內建種族按繁殖類型分組後附進提示詞，讓模型在寫 `base.race` 與 `bsAddSperm.race` 時有詞彙表可依。此前模型完全看不到有哪些種族，只能自創名稱，而自創種族在系統內查不到生理參數、會靜默套用預設值。追蹤請求只附名稱（約 350 token），註冊請求另附極短辨識提示（約 1000 token）。純現代寫實劇本可關閉。
- 哥布林的男胎比例由 99 調整為 95，保留雌性哥布林的敘述空間。
- 新增七個內建種族：月兔族與杜拉罕（胎生）、狗頭人（卵生）、梅杜莎（卵胎生）、修格斯（胎轉卵生）、活體鎧甲與偽人（不定型），共 67 族。
  - 狗頭人與哥布林同生態位但產卵，分娩負擔遠低於哥布林；梅杜莎是蛇人的妖化分支，比蛇人難受孕、孕期更長；活體鎧甲走宿主孵化路線（受精容易、排卵多），與人類造物組相反；偽人的同卵分裂傾向極高，其餘生理刻意貼近人類。杜拉罕的軀體不依賴頭顱運作，是胎生組唯一「難受孕但承載力強」的種族——該組其餘成員都是越難懷越扛不住；奇幻向的不死詮釋寫作 `[不死]杜拉罕` 即可，不需要另開種族。
- 衍生類型「修行」拆為東方的「修煉」與新增的西方「魔導」（魔力／不限性別／遺傳速度 1.0）。舊存檔寫著 `[修行]XXX` 的角色會自動解析到修煉，繁體寫法一併映射，不會失去 flux 與代謝抵免。
- 天使與惡魔的經期改為約一年（全表最長）。搭配上述排卵修正，長週期不再造成卵數虛增。
- 提示詞不再把追蹤對象限定為女性：`female` 欄位的語義明確為「孕育者」——被追蹤的承載方，不限定性別。扶她、孕夫、雄性孕育系（如海龍人）皆使用同一欄位。此前註冊、追蹤、備裝三處的系統提示詞都寫死「女性角色」，模型會抗拒或改寫這類設定。欄位名維持 `female` 不變。

### 修正

- 種族生理區塊新增「承載耐受」一行，並改用敘事化描述（如龍族「妊娠幾乎不削弱她，照常行動、勞作乃至戰鬥都在能力範圍內」、精靈「孕期負擔沉重，行動力明顯下降」）。此前這個數值雖然驅動胎兒能耗、妊娠阻滯、代謝加速、宮壓累積與產程判定，卻從未出現在任何提示詞裡，模型只能照「孕婦即虛弱」的常識描寫。混血平均與妊娠偏移區塊一併顯示。
- 修正承載耐受的妊娠偏移方向：偏移只取母體自身耐受 × 胎數修正，不再乘上胎兒種族的耐受。此前人類懷龍胎會得到十倍耐受、比懷人類胎還輕鬆——`breedTolerance` 描述的是「這具身體多能扛妊娠」，屬於承載者，胎兒種族的承載力不該變成母體的加成；跨種族的額外負擔本來就已由受精時的胎重換算表達，這裡等於反向重複計算。連帶影響：人類產下龍胎的恢復期會明顯拉長，產下袋獸胎則大幅縮短。
- 宿主代理回 401／403（多半是 CSRF token 失效）時，本次工作階段改為直接走直連，不再每次請求都先撞一次代理。此前每一輪追蹤都會先向 ST 後端發一次注定失敗的代理請求、再靜默回退直連成功，使用者端看不出異常，但 ST 的 console 會被 `ForbiddenError: Invalid CSRF token` 持續灌爆。熄火時會在瀏覽器 console 留一則說明，硬重整頁面即可恢復代理。
- 修正 `after_ai` 搭配串流輸出時，一輪會發出兩次追蹤請求的問題。宿主在串流開始時會先補一個空的助手樓層，內容要過一陣子才寫進去；串流守衛只比對內容有沒有變化，空字串「穩定」地撐過判定時間就被誤認為串流已完成，於是對著空白樓層先發一次請求，內容真的寫入後再發一次。空白助手樓層現在一律視為尚未就緒。
- 修正 `after_ai` 觸發時機下，使用者自己送出訊息也會彈出「追蹤更新中／追蹤完成」的問題。那一輪只是記一筆 skip 快照、不會發出任何請求，屬於純記帳流程；提示改為只在確定要送出追蹤請求時才顯示，整輪都是記帳時全程靜默。此前浮球在這種情況本來就不閃，是新加的提示沒跟上這個約定。
- 修正自然排卵的總量算法：單個排卵期排出的卵數改為「1 顆基礎 + 額外排卵傾向」，不再隨經期長度變動。舊算法是「每天至少 1 顆 × 排卵天數」，而排卵天數隨經期倍率線性拉長，於是長週期種族按窗口長度虛增——精靈的額外排卵傾向是 0，實際上每週期卻排 6 顆；龍族排 8 顆。人類、社會蟲族等短週期種族的卵數不變。
- `bsAddSperm` 不再接受負數（扣除請用 `bsDrainSperm`）；`bsChildbirth` 限定已進入妊娠階段才可執行；假孕期無胎兒時 `bsAbortion` 不再誤記為流產；`bsSetCharacterPresence` 的 `isPresent` 改為必填，模型漏填不再默認為在場。
- 修正模型吐出 `constructor`、`toString` 等保留名當角色名時整輪追蹤拋錯的問題。這類名稱原本會取到繼承來的內建屬性、被當成角色物件處理，拋出的異常會使該輪追蹤失敗並擋住自動重試；現在一律視為未知角色跳過。
- 修正性慾下降時誤觸發泌乳（原本取了絕對值，把下降也當成上升）。
- 修正註冊胎兒種族被二次混血：模型未顯式提供父系時，胎兒種族原樣保留，不再與承載者的種族再混一次。代孕與移植胚胎不會再被硬塞進承載者的血統。多母源與嵌合體的來源欄位在註冊時一併保留，手動轉交不再失去歸屬依據。
- 極端時間推進不再凍結介面：妊娠代謝的症狀抽樣與逐日步進各自加上迴圈上限。時間推進與階段推進維持原語義，只有荒謬的時間跳躍會被截斷。
- API Base 校驗：公網 http 位址拒絕（避免 API Key 明文傳輸），本機與內網位址照常放行；非 http(s) 協定一律拒絕。錯誤訊息中的 key/token 會脫敏後才顯示。
- 宿主代理失敗時的回退條件新增 5xx；429 不回退，避免對已限流的端點翻倍施壓。
- TauriTavern 下翻頁期間切換聊天不再把兩個聊天的樓層混進同一份快取，並限制重試次數。
- 偵測到 DeepSeek 系模型時，格式化輸出會額外注入輸出結構指令（僅追蹤流程）。使用者關閉「格式化輸出」開關時一律不啟用，不會被模型判定覆蓋。
- 混血性別比：雙性（`genderRatio` 為空）不再被數值平均抹掉，只要成分中有雙性種族，後代即為雙性；無性（-1）維持讓位給數值平均，避免混血後代全數絕育。
- 同基種族帶不同裝飾子項（如「獸耳族-兔x獸耳族-貓」）在混血運算時歸併為單一成分，不再雙重加權。
- 混血包含未收錄種族時，提示詞會標明平均數值不完整，不再靜默丟棄該成分。
- 妊娠生理偏移的提示詞數值改為與工具側一致：妊娠長度取胎兒種族的天數平均、分娩難度不按胎重加權，且兩者均由胎兒種族決定，不再疊乘母體基準值。先前非人類母體（如精靈）顯示的妊娠長度可能與實際推進差一倍。
- 種族名、衍生類型、註冊推演的使用者輸入在拼入提示詞前會剝離換行與閉合標籤，避免內容截斷規則段落。
- TauriTavern 下設定讀取失敗不再讓主題切換整個失效（「點了沒反應」）。
- 設定項下方的小字說明改用主題輔助文字色，修正淺色主題下幾乎不可讀的問題。
- 主流程快照改為綁定所屬聊天：切換聊天後不再沿用上一個聊天的快照內容，未綁定的舊格式快照一律視為失效。
- 注入主線的已註冊角色狀態改為轉義後序列化：角色卡或註冊內容含 `</bs_biotracker>` 等字樣時，不再提前閉合包裹標籤而把後續文字當成指令送進主模型。

## v0.9.4

### 新增與調整

- 新增受精卵早期融合（嵌合體）機制。角色體內有多顆尚未著床的受精卵時，`fertilizationDays > 1` 後每一對只判定一次；機率同時參考兩顆胚胎種族的 `identicalProbability`、`impregnationDifficulty`、胚胎系統及父系 `derivedType`。不同種族可以融合；不同胚胎系機率降為 0.25 倍；只有一方有衍生類型為 0.5 倍；同衍生為 1.5 倍；不同衍生不能融合；最終機率上限 75%。
  - 融合後保留並合併完整的裝飾種族、父源與母源，胚胎類型重新依混合種族中最低 `gestationSpeciesSpeed` 的既有規則決定。嵌合體不可再次融合，但仍可同卵分裂。
  - 同性來源沿用原性別；異性來源先記為「待定」，著床時每一胎獨立按男 40%／女 40%／雙 20% 決定，因此同卵分裂後也可能不同性別。
  - 雙父以 `×` 合併顯示。多母源孩子預設登記在實際孕母名下，並可在完整變量頁手動轉移給任一已注册母源；單一 provider 的既有自動轉交不變。
- `bsImplantEmbryo` 改為加入「尚未著床的外源受精卵」，不再直接進入孕早期。第一顆自然或外源受精卵會啟動共用 `fertilizationDays`，同一窗口可繼續加入其他來源的胚胎且不重置時鐘；實際著床時才增加懷孕經驗。

### 修正

- 追蹤器現在可辨識 `operations`、`actions`、`toolCalls` 與 OpenAI function-call 包裝的工具結果，並統一轉為可執行的 `tool_calls`；不再出現面板已顯示新 JSON、但沒有套用的情況。
- 強化角色回場／離場提示，明確要求 `bsSetCharacterPresence` 使用 `female` 與布林 `isPresent`；離場角色在追蹤清單中不再被禁用，可進入資料頁並手動切回在場。
- 角色追蹤頁新增「優先追蹤角色名」輸入框，支援逗號或換行分隔。填寫後會優先檢查指定角色，但不排除其他已註冊角色；留空時平等追蹤全部已註冊角色。
- 單次追蹤回應新增逐角色 `character_checks` 清單，要求每名已註冊角色各有一筆檢查結果；最新結果面板會顯示預期、已檢查與缺漏角色，實際狀態更新仍只接受工具呼叫。
- 注册与繁育推演目标名支持 ST user 宏：`user`、`{user}`、`{{user}}`、`<user>` 会统一解析为当前聊天的 user 名，再用于提示词、payload、回传 JSON、注册 state key 与套用操作；普通角色名保持原样。
- 修正繁育推演可能把 `target_character` 写成其他角色名：提示词会明确锁定本次已解析目标，回传 JSON 与手动编辑结果也由程序端强制对齐。推演分页新增“本次推演目标”提示，手机滚到结果区时仍可确认正在操作哪个角色；“套用”按钮改为明示仅限已注册角色，新角色则引导到“注册”分页完成注册。
- 完整變量頁的代孕／寄生孩子手動轉交，現在只能移交給該筆資料 `provider`／`providerSources` 指定且已注册的歸屬方；執行層也會拒絕任意目標，避免錯誤轉交後清除來源標記。
- 修正代孕與精液來源的衍生類型解析：`bsAddSperm.race`、`bsImplantEmbryo.race` 與 `fatherRace` 均沿用 `[derivedType-裝飾子項]race-裝飾子項` 描述符及 `X` 混血格式。母系固定取孕母；父系優先取 `fatherRace`，未帶衍生類型時退回卵源 `race`，不再依名稱字串查找角色。
- 修正帶裝飾子項的混血解析：`獸耳族-兔x精靈-木` 現在會保留兩個完整血統成分供存檔與胎兒顯示，並分別以 `獸耳族`、`精靈` 參與種族生理、同族判定與胚胎類型運算，不再於第一個 `-` 截斷後半混血。

## v0.9.3

### 新增與調整

- 真實產程（realisticLabor）下的產兆前驅只能延後、不能取消：累計延後上限為初始時長的 100%，達上限後必然進入產程，不會回退至妊娠階段。宮壓與性慾仍依既有倍率影響產程速度。
- 新增 `bsImplantEmbryo`，用於孕育者不是遺傳母親的情節：代孕、胚胎移植、蟲母注卵、寄生產卵。
  - 胚胎種族依遺傳母方推導而非承載者，蟲母的卵放進人類宿主仍是蟲族血統。`provider` 尚未注册時用 `race` 指明遺傳母方，父方預設同族、跨種族時用 `fatherRace`。
  - 承載者已懷孕或已受精、或 `provider` 等於承載者本人時會被拒絕。
- 完整變量頁新增「搬移孩子記錄」：可把代孕或寄生所生、仍留在承載者名下的孩子搬給真正的家長。
  - 只開放帶 `provider` 的孩子；自然生育的歸屬不可搬移。搬到位後會清除 `provider` 標記。
  - 搬移會同步修正指向該筆記錄的「注册來源」，已注册孩子的來源不會因索引位移而失聯。
- 新增 `bsRuptureMembranes`，作為破水的唯一入口。
  - 僅限產兆前驅且宮壓達上限 66%，或第一／第二產程；臨產期與逾期不能跳過產兆前驅。產兆前驅破水後直接進入第一產程。
  - 未確認破水前，系統會明確禁止描寫羊水流出。該工具只在可破水的階段提供給模型。

### 修正

- 同族生育不再得到「人类x人类」這種自我混血種族；跨種族混血不受影響。
- 代孕／寄生所生的孩子不再消失：會轉交給 `provider` 指向的已注册角色；對方尚未注册時留在承載者名下，並以 `children[*].provider` 保留歸屬。
- 注册角色時一律採用使用者輸入的名字，不再被模型改成角色卡名。
- 技能／天賦寫入時，圖鑑中找不到的引用會被跳過並列出，其餘照常寫入，不再整組丟失。提示詞補上「天賦需要對應的已定義技能」與輸出前自檢；圖鑑為空時會要求逐一定義。
- 繁育推演換角色後不再顯示上一個角色的結果：推演開始與失敗都會清空編輯器，改角色名時一併清除。
- 分析途中刪除或改寫訊息不再造成失控。
  - 結果對應的訊息若已被刪改，整份作廢，不寫入狀態也不留下快照。
  - 對話沒有變化時不再重複自動重試同一個失敗請求；一有變動即恢復。
  - 快照比對加入邊界訊息校驗，長度相同但內容已變時不再誤判為匹配。
  - 找不到可用快照時只回放最近 `contextSize` 樓，不再從第 0 樓重跑整段對話。
- TauriTavern／Luker 上重開存檔不再可能清空已註冊角色：確認過該聊天的存檔內容之前，不允許以空狀態回寫 sidecar。使用者主動清除不受影響。
- TauriTavern 上重開存檔能正確讀回已註冊角色：store 尚未就緒時會等待並自動重試，開啟小手機時也會補一次載入。
- TauriTavern 首次進入聊天不再彈出紅色「後端錯誤：Failed to get chat store json … Not found」。宿主未提供 store 列舉方法時，行為與先前相同。

## v0.9.2

### 修正

- 修復追蹤卡在「正在分析」永不返回的問題：請求若因宿主代理或渠道掛起而不回應，過去沒有任何超時，`fetch` 會一直懸著，運行鎖不會釋放，之後所有手動／自動分析都被「已有一輪追蹤請求正在執行」擋掉，且重啟容器或插件也無法恢復。
  - 所有 API 請求（追蹤、注册、模型列表，含宿主代理與直連兩條路徑）改用 `AbortController` 加上超時終止，逾時會拋出可讀錯誤並彈 toast。
  - 新增「請求超時（秒）」設定，預設 180 秒，可填 0 表示不限制；模型列表探測最多等 60 秒。
  - 超時歸類為不可重試錯誤，避免自動重試把卡住的時間乘上重試次數。
  - 運行鎖加上看門狗：超過「超時 + 120 秒」仍未推進即視為死鎖並放行下一輪；鎖改為 token 制，逾期的舊輪次結束時不會誤清新一輪的鎖。多條消息連續處理時每條都會刷新心跳，長隊列不會被誤判。
- 修復關掉小手機再打開，繁育推演結果與注册狀態全部清空的問題：弹窗只是隱藏而不銷毀，但每次打開都會無條件重置注册頁，推演草稿被清成 null，套用時提示「沒有可套用的繁育推演」，只能重跑一次推演。
  - 現在只有切換聊天才會回到初始文案；推演草稿改為在該角色注册成功後才清空。
  - 注册／推演／備裝／日記／技能五個異步操作統一登記為「進行中」，重開小手機會還原按鈕的禁用狀態與「注册中…」提示，不再顯示成空白。
  - 注册按鈕過去完全沒有節流：關掉再打開後按鈕看起來可以按，實際上前一輪還在跑，連點會重複注册同一個角色。現在進行中的操作會擋下重複點擊並提示。
- 追蹤「經歷」分頁的技能區塊改為方格墻：每行三格，自動換行並一次顯示全部技能。
  - 等級以羅馬數字呈現，數字填色顯示經驗進度；字形會依主題字體自動縮放並置中。
  - 天賦以右上角楔形角標呈現：朝上為擅長、朝下為苦手，楔形數量代表等級；滿級技能會提亮。
  - 天賦等級上限為 ±Lv5，相關提示、編輯與校驗同步採用此範圍。
- 技能分頁的左右切換會正確顯示所有技能。
- 修復切換排卵周期後改不了完整變量的問題：`base.days` 是階段內的 0 基天數（注册與每次階段切換都寫 0，界面再按「第 N+1 天」顯示），但完整變量的校驗要求 `>= 1`，於是剛切換階段的角色一按「應用修改」就報「profile.base.days 必须大于等于 1」。校驗改為只擋負數。

## v0.9.1

這版修正世界書傳輸漏掉角色附加知识书的問題、修復數据库 skill 化條目的排除匹配，並消除 TauriTavern 上新聊天的「後端錯誤」彈窗。感謝使用者回報與提供改動包參考。

### 修正

- 世界書收集新增角色附加知识书（charLore／extraBooks）：以頭像檔名或卡名匹配當前角色，讀取來源涵蓋 world-info 模組、酒馆助手 `getCharLorebooks`、`world_info` 執行期全域等多源回退。tracker 與注册 payload 會將附加書經角色側排除名單過濾後與全域書合併傳輸（按書名去重），並附上 `character_additional_worldbook_names`。此前附加知识书在任何宿主上都不會被傳輸。
- 全域啟用書名同樣改為多源回退：world-info 模組 `selected_world_info` → 執行期全域 → `world_info.globalSelect` → 頁面多選框 → 酒馆助手 `getLorebookSettings`，讓酒馆助手 iframe 注入等 import 失敗的環境也能讀到書單。
- 數据库 skill 化條目修復：多行 comment（含 `ACU_SKILL_META` 注釋塊）統一清洗成單行標題再顯示與匹配；排除／白名單「書名 :: 條目名」與裸條目名互認；載入世界書時同步以清洗後名稱過濾。過濾頁為 skill 化條目加上 `skill` 標記，手機 WebView 勾選補 `pointerup` 兜底。
- 過濾頁改版：角色分頁改為「角色世界书(主+附加)」，附加知识书條目以「書名 :: 條目名」顯示並走角色側名單；全域分頁維持全域書。
- 新增「格式化输出(v4兼容)」開關（預設開）：關閉後請求不再附加 `response_format: json_object`，兼容不支援該參數的渠道。
- 異步請求新增全局自動重試：失敗後按 1s/2s/3s 間隔最多重試 3 次（401/403/未配置不重試），每輪內保留既有的 JSON 糾錯請求。
- TauriTavern：per-chat store 讀不到 `chat-state-v1`（新聊天首次探測）時記住該聊天暫無存檔，後續不再重複探測；併發探測合併為單次請求。避免宿主把例行的 Not-found 當後端錯誤反覆彈窗，插件保存狀態後會恢復正常讀取。根治仍需宿主對 store 讀取 miss 回傳 null 或提供存在性檢查。
- UI harness 預載全域書、附加知识书與 skill 化條目 mock，供過濾頁手動驗收。
- 追蹤「經歷」分頁精簡技能顯示：技能卡只保留等級與 EXP 進度條；天賦改為對應技能卡右側小標（僅顯示方向與等級，有天賦無技能則不顯示）；移除「先天天赋」與「技能成长记录」區塊，成長記錄改為只在技能頁的角色管理中查看。胎兒天賦同步精簡為「技能名(Lv±N)」，不再顯示經驗。

## v0.9.0

這版加入 ERA 風格的動態技能與跨世代天賦傳承。

### 新增與調整

- 新增目前聊天專用的全局技能圖鑑，以及首頁燈泡入口；`bsRegisterSkillDefinition` 新增技能時必須同時定義名稱與描述，角色資料只保存穩定的技能 ID。
- 新增 `bsTrainSkill`：LLM 依故事強度直接給予非負 `skillExp`。技能從 Lv1 覺醒、只進不退；角色天賦對所有 LLM 工具只讀，工具 schema 不提供 `talentExp`，執行器也會硬拒絕舊式參數。角色天賦只能由使用者透過注册、技能或變量等外部介面調整；工具只能依孕期親和度自動改變胎兒天賦。
- 技能與天賦採用 `requiredExp(level) = 100 * level * level`；技能 Lv1→2 需要 100、Lv2→3 需要 400，天賦 Lv0→擅長／苦手 Lv1 都需要 100。
- 孕中期、孕晚期、臨產期、逾期、產兆前驅與第一產程的技能鍛鍊，每次只會隨機選擇一胎，依該胎親和度換算天賦經驗；第二、第三產程不再傳遞。換算為 `round(skillExp * abs(affinity) / 50)`，方向由親和度正負決定。
- 胎兒只保存天賦；分娩轉為孩子時會完整保留天賦，並新增出生胎重倍率 `birthWeightRatio` 與出生親和 `birthAffinity` 快照。
- 角色注册頁新增「註冊來源」，可選擇任一已注册角色尚未註冊的孩子；選擇後固定種族／衍生種族、把出生資料與具名天賦交給註冊模型參考，並確定性繼承原天賦。完成後以 `registeredAs` 防止同一孩子重複註冊，註銷角色時會釋放連結。
- 角色注册新增第五子頁，可用全局圖鑑的 ID 或精確名稱設定初始技能／天賦；孩子來源的既有天賦在此頁只供技能生成參考，不會被模型覆蓋。
- 技能頁新增已注册角色管理：可從全局圖鑑加入或移除個人技能／天賦，並直接調整 Lv 與 EXP；全局技能只有在角色、胎兒與孩子均未引用時才可刪除。
- LLM 回報技能鍛鍊時必須提供事件說明；若結算後發生覺醒或升等，系統會將該說明自動寫入 `profile.skillHistory`，保存 skillId、前後等級、故事／手動來源與時間。一次跨多級只記一筆，最多保留最近 100 筆；`bsTrainSkill` 同時回傳 notify 供 tracker 提醒使用者，單純增加 EXP 與天賦變化不記錄。
- 全局技能圖鑑與 `nextSkillId` 納入聊天快照，回溯角色狀態時會同步恢復對應定義；技能 ID 改為單調遞增，刪除後永不回收，避免舊快照與操作紀錄產生同 ID 異義。
- 衣櫃頁新增外部手動管理：未備裝角色可建立空衣櫃，已備裝角色可新增、編輯或刪除主件／配件，調整四維、層級、組成部件，以及套用目前主件、配件與 `wearState`。所有操作沿用既有衣櫃工具驗證並建立快照。
- 角色追蹤的經歷、妊娠與孩子卡片支援技能／天賦顯示；存檔正規化、快照、幕外角色上下文與 tracker payload 同步支援新欄位，並遷移早期試作的 `inheritedTalents`。
- 新增模型、工具、快照與 UI 契約測試，覆蓋技能圖鑑、ID 配置器、升等歷史／notify、經驗曲線、天賦逆轉、親和度四捨五入、完整產程允許矩陣及分娩邊界；UI harness 預載技能、天賦與成長紀錄供手動驗收。

## v0.8.5

這版改善換裝追蹤：補上換裝觸發規則與衣著一致性檢查，並讓幕外角色回場當輪即可正確換裝。

### 新增與調整

- 改善换装追踪：追踪提示词加入换装触发规则与「衣着描述必须与 outfit 一致」的检查约束；payload 中的 outfit 附带系统解析的当前穿着摘要 currentWearText（主件 + 配件名），叙事与机械穿着不一致时模型可直接察觉。
- 幕外角色现在也会发送精简衣柜（仅 id/name/slot）与当前 outfit 摘要，角色回场当轮即可正确调用 bsChangeOutfit 完成换装，不再慢一拍或猜错衣物 id。
- 衣柜工具支持按名称引用衣物：bsChangeOutfit / bsRemoveWardrobeItem 的衣物参数可传整数 id 或准确名称字符串（含临时衣物与「全裸」）；bsAddWardrobeItem 可省略 id 由系统自动分配下一个整数 id，名称匹配既有衣物时视为更新，不再因字符串 id 被 hash 成大数字而找不到或产生重复衣物。
- 四维数值改为孕期窗口专属发送：角色不在真妊娠/产兆前驱/产程/产后恢复时，payload 中的衣物瘦身为 id/name/slot/note（note 承担非孕期敘事所需的稳定外观），四维仍完整保存在持久化状态中，进入孕期窗口后自动恢复全量发送。
- pregFit 窗口扩展到产后恢复：产后衣着压力从产后初期水平随恢复进度线性递减到 0，覆盖体型未回、乳胀消退阶段的衣着冲突敘事；此前产后恢复期间 pregFit 直接为 null。
- 新增 outfit.wearState 穿着状态短标签（12 字内，默认 整齐）：建议词表 整齐/凌乱/敞开/半褪/撩起/上衣已褪/下装已褪/湿透，也可按情境自造；bsChangeOutfit 可只传 wearState 更新状态，换主件未显式传入时自动重置。「脱了一半」类持续状态从此有机械表达。
- 主件可附 parts 数组（组成部件名）；配件可附 layer=inner/outer 区分贴身衣物与外搭，备装默认生成 1-2 件贴身衣物配件。仅着内衣可表达为 mainItemId: 0 加 inner 配件；currentWearText 按层次渲染（含「仅着：」「内着」与穿着状态后缀）。
- bsChangeOutfit 新增增量配件参数 addAccessoryItemIds/removeAccessoryItemIds：穿上鞋、脱下外套等单配件变化不再要求整表重述 accessoryItemIds（实测模型常因此只改 wearState 而漏掉配件穿脱）；覆盖式 accessoryItemIds 保留用于整套重设，两者同传时以覆盖式为准。
- 明确外层建模规则：毛衣、开衫、外套等可独立穿脱的外层应为 layer=outer 配件而非 main 的 parts，备装与追踪提示词同步约束，并允许追踪把误并入 main 的外层拆成配件；wearState 在主件有 parts 时建议引用部件名消歧（如「毛衣已脱」）。
- 上下装混搭改为惰性组合：重新搭配时用 bsAddWardrobeItem 铸造新组合主件（parts 列出部件）再换上，不预先枚举组合。
- 移除 衣着动态/衣着自评 描述子字段：实测与机械穿着脱节，其职责由 wearState 与 currentWearText 承担。备装不再生成与要求这两个字段，既有存档载入时会一次性剥离对应子字段（含繁简变体），衣柜 UI 改为显示主件/状态/配件/贴身。
- 修复描述缺失：bsSetDescription 收到空字符串补丁时不再清空整个描述栏位（此前模型把“不改”表达成空串会静默抹掉所有子字段）；改为 no-op。
- 修复妊娠描述死锁：角色进入妊娠相关阶段而 pregnantDescription 为空时，追踪提示词会注入 [pregnantDescription 初始化] 段（含描述规范与点名角色），并在「不要新增描述子字段」规则中开出对应例外；此前追踪模型从未收到孕态描述规范，且被该规则锁死无法建立首批子字段，只有注册页胚胎流程能写入。
- 新增 tests/wardrobe.test.mjs 与 tests/tracker_pipeline.test.mjs：覆盖衣柜工具行为、id/名称解析、wearState、产后 pregFit、存档剥离，以及 payload 组装 → 系统提示词 → 模拟模型回应 → 全管线套用的灰度链路；tests/ui-harness.html + tests/ui_server.mjs 提供 mock 宿主的前端可视化验证环境。
- 内部重构：衣柜/穿着的规范化逻辑（衣物字段、id、配件补正上限、临时衣物）统一收敛到新模块 wardrobe_config.js，state.js 与 tools.js 不再各持一份近似实现；状态载入时的衣柜清单现在也会按 id 去重，与工具层行为一致。

## v0.8.4

這版新增 Luker 宿主相容與多 API／Agent 編排控流，避免中間輸出過早觸發追蹤。

### 新增與調整

- 新增 Luker 宿主兼容：优先使用 Luker context 与 per-chat sidecar 保存状态，并默认开启多智能体安全模式，编排完成后由用户手动分析；TauriTavern Agent 提交会等待对应 run_completed 才触发自动追踪，取消或失败保留手动分析入口。
- 悬浮球支持长按隐藏；隐藏后可从扩展程序菜单重新打开手机面板。
- 修正注册时描述栏位为空会使后续 `bsSetDescription` 无法建立首个子字段的问题；胚胎状态更新现在可写入首段孕态描述。
- 修正 TauriTavern 的扩展入口恢复：只认定实际魔杖菜单中的 BioTracker 项目，避免宿主 API 的隐藏占位误判；持续监听魔杖菜单建立与重建，确保第三方扩展加载后项目仍会插入；没有确认恢复入口时，长按不会隐藏悬浮球。
- 修正 TauriTavern 可能在第三方扩展载入前已发出 APP_READY，导致插件未执行初始化的问题；现在保留幂等保底启动。

## v0.8.3

這版將備裝與繁育心理改為明確的按需功能，並加入獨立日記流程與追蹤提示詞體積警示，減少不需要的工具與上下文 token。

### 新增與調整

- 左上角电池改为只计算 BioTracker 注入主流提示词的文本；优先使用宿主 tokenizer，宿主未提供或计算失败时才回退本地估算，并在提示文字标示来源。
- 备装与衣柜换装提示统一将 main 视为完整基础套装；一般上衣与下着必须合并为同一 main，并按整套评估四维。
- 注册完成后会停留在注册页面，方便继续为角色备装或写日记，不再自动跳转角色追踪。
- 衣柜只显示已完成备装的角色；未备装角色不会进入衣柜详情页面。
- 新增「描述完整更新模式」开关：开启后提示词要求 bsSetDescription 每次回传该描述栏位的全部既有子字段；一般模式也会要求先逐栏检查，避免因求简短漏改受剧情影响的描述。
- 未完成繁育推演的角色，其「心情」页面会隐藏繁育心理雷达、阶段提示与旗标，只保留日记内容。
- 注册流程不再生成首篇日记或传输日记写作规则；日记仅能在独立日记标签生成与写入。
- 繁育推演兼容模型将推演结果包进 psychology 或 profile.psychology 的 JSON 结构，避免已回传阶段表却被误判为缺失。
- 未推演角色不再向追踪请求发送心理状态或心理说明；衣柜与心理工具也会在执行层确认目标角色已备装／已推演，避免跨角色误用。
- 追踪请求在没有任何角色完成繁育推演时隐藏 bsUpdatePsychology，避免发送无可用阶段人设的心理更新工具。
- 直接注册不再生成或套用繁育心理与 6×6 阶段人设；只有先完成并套用繁育推演时，注册请求才会携带该区块。
- 左上角电池改为最近一次追踪提示词的本地 token 估算；默认 4096 token 注意力预算可在系统设置调整，超出仅显示耗尽，不阻止请求。
- 注册页面新增独立“日记”标签，可按写作规则与可选故事日期调用 AI 生成日记预览，修改后再通过既有日记规则写入角色状态。
- 百科拆分为一般类型与衍生类型子页；衍生百科现可覆盖短敘述、Flux 描述及遗传速度，代谢抵免维持内置规则，自定义衍生子项会继承所属大类的有效参数。

## v0.8.2

這版新增 TauriTavern 支援，並將宿主資料操作集中至薄型 adapter，降低追蹤核心對 SillyTavern 內部結構的直接依賴。

### 新增與調整

- 新增宿主 adapter，集中封裝 context、聊天窗口、角色列表、聊天 ID、擴充設定、生成預設、世界書與宿主事件訂閱。
- 在 TauriTavern 中透過 `windowInfo` 與 `history` API 按需建立絕對索引聊天視圖，避免將窗口內 index 誤當完整聊天樓層。
- TauriTavern 使用 `stableId` 識別聊天，並以 per-chat `store` 作為聊天追蹤狀態的持久化來源；`extensionSettings` 只保存全域插件設定。標準 SillyTavern 仍維持原有 `extensionSettings.chatStates` 儲存方式。
- TauriTavern 的狀態清除以當前聊天為單位；因宿主未提供跨聊天 store 枚舉，介面不會執行無法保證完整性的「清空所有聊天」。
- 統一 World Info prompt、角色／全域世界書與 preset 的 fallback 存取，並集中處理聊天建立、切換、刪除等事件的重複訂閱與異步錯誤。
- 新增雙宿主回歸測試，覆蓋標準 SillyTavern 設定保存、TauriTavern stableId／per-chat store、窗口歷史絕對索引與宿主 API fallback。
- 修正自動追蹤請求失敗後會隨 poll 對同一訊息持續重試的問題；同一訊息失敗後會停止自動重送，新訊息會自然解除，手動分析仍可再次嘗試。
- SillyTavern 的「清空所有聊天」改為只統計真正含有追蹤資料的聊天，並順帶清理空白殘留 key；TauriTavern 則隱藏宿主無法完整執行的全聊天清除入口。

## v0.8.1

這版改善備裝流程與 API 設定的容錯，減少角色名稱差異、模型列表端點與 AI 漏填欄位造成的操作中斷。

### 修正與調整

- 備裝與追蹤目標的角色名稱比對現在會忽略大小寫、前後空白與連續空白，並可依已註冊狀態中的角色名稱解析實際鍵值。
- 備裝 AI 漏填 `outfitDescription` 或 `outfitSelfView` 時，會依目前選定的主件與配件生成保底描述，仍可完成套用。
- API Base URL 若誤填到 `/chat/completions` 或 `/models`，插件會自動移除 endpoint 尾巴。
- API 不支援 `/models` 時，模型列表錯誤訊息會提示可手動填寫模型名稱；追蹤與註冊仍只要求 `/chat/completions` 可用。

## v0.8.0

這版新增妊娠服裝系統，讓角色的衣櫃、當前穿著與孕期體型變化能一起參與追蹤與敘事。

### 新增

- 新增角色專屬 `wardrobe` 與 `outfit` 變量組；角色完成備裝後，才會啟用服裝相關 prompt 與 tool。
- 手機首頁新增「衣櫃」入口，可查看已備裝角色的衣櫃、當前主件與配件。
- 註冊頁新增「備裝」分頁，可讓 AI 根據角色卡、世界書、最近對話與已註冊狀態生成衣櫃 JSON，並可手動微調後套用。
- 新增衣物四維：`masking`、`support`、`capacity`、`convenience`，分別描述遮掩輪廓、承托、容身與行動／穿脫便捷性。
- 新增 `outfit.pregFit`，在真妊娠、產兆前驅與產程中依孕期進展與胎兒負荷計算 `pregWearPressure`，並生成四維 `gap` 供敘事判斷衣物是否吃緊。
- 備裝結果新增 `outfitDescription` 與 `outfitSelfView`，會分別寫入 `normalDescription` 的「衣着动态」與「衣着自评」欄位。
- 新增 `bsAddWardrobeItem`、`bsRemoveWardrobeItem`、`bsChangeOutfit` 三個工具，支援新增衣物、刪除衣物與更換當前穿著。

## v0.7.2

這版是小幅介面整理，讓完整變量頁承接全角色時間推進，也補上角色在場狀態的調試切換。

### 新增與調整

- 將全角色時間推進整合進 `完整变量` 頁面，不再保留獨立時間入口。
- 角色調試工具新增「在場」切換，可快速標記角色在場或離場。
- 舊的 `time-lapse` 頁面狀態會自動導向 `full-state`，避免舊 localStorage 記錄造成空頁或錯誤入口。

## v0.7.1

這版調整妊娠天數語義，使系統更符合產科定義。

### 調整

- 將 `pregnant.pregnantDays` 定義改為孕齡／產科孕周天數，等同從末次月經或本族等價週期起點開始計算。
- 受精後著床等待期改為依角色實際月經週期長度縮放，不再固定 6 天；實際週期會同時考慮種族倍率、活力等級與情壓等級造成的週期波動。
- 著床成功進入孕早期時，會以「受精後天數 + 實際週期一半」作為初始孕齡，並同步換算 `effectivePregnantDays`。
- 追蹤頁與調試欄位將妊娠天數顯示改為「孕齡」，避免和受精後實際胚胎天數混淆。
- 更新註冊與 tracker 變數說明，明確區分「孕幾週／懷孕幾週」與「受孕後／胚胎發育幾週」。

## v0.7.0

這版新增角色繁育心情推演，讓角色在正式註冊前先建立更貼合個性與經歷的繁育心理底盤。

### 新增與調整

- 角色註冊頁拆分為「推演」與「註冊」分頁；可先執行繁育推演，再用推演結果註冊角色。
- 繁育推演會根據角色卡、世界書、最近對話、種族與補充設定，判斷角色目前應使用月經期心理或孕期心理起始值。
- 推演結果會暫存於註冊流程；若角色名、種族或補充設定未變更，按下註冊時會自動注入該次推演。
- 已註冊角色可直接套用繁育推演；若角色尚未註冊，介面會提示改用註冊分頁完成注入。
- 繁育推演結果可在文字框中手動微調 JSON，再註冊或套用。
- 新增角色專屬 `psychology.stageProfiles`，保存 6 條心理軸 × 6 個階段區間的自訂詮釋，避免階段切換後退回通用預設。
- 推演 prompt 要求同時產生月經期與孕期的完整 6x6 階段表，並禁止直接套用預設階段標籤。
- 註冊與心理更新流程會保存並沿用角色專屬階段詮釋，使後續 `bsUpdatePsychology` 能在同一套心理底盤上推進。
- 繁育推演會同步判定 `isChaste`、`hasContraception`、`knowsFatherSource`、`hasProfessionalPrenatalCare` 等目前狀態布林欄位。
- 角色追蹤頁的心情雷達圖下方新增當前階段提示，顯示目前數值對應的角色專屬心理詮釋。

## v0.6.1

此版主要為 Debug 修正。

### 修正

- 修正階段進度與妊娠天數的顯示邏輯，使追蹤頁以第 1 天起算呈現目前階段。
- 修正混血／複合種族的妊娠速度合併方式，改以平均妊娠天數換算，避免速度倍率直接平均造成偏差。
- 修正胎兒種族推導時會去重複組成的問題，保留父母雙方種族組合以反映實際混合來源。
- 調整多胎妊娠生理計算，妊娠速度與分娩難度改以胎兒個體平均處理，降低體重權重造成的不合理偏移。

## v0.6.0

這版大幅重整代謝需求系統，使需求更貼近日常循環與妊娠症狀互動。

### 新增與調整

- 將尿意與便意合併為 `excretion`（泄意），將 `milk` 統一表述為乳意，並新增 `companionship`（伴意），形成泄、餓、困、乳、臭、伴六項需求。
- 新增妊娠 `acceleration`（快積）症狀，與 `blockage`（阻塞）分別表現為加速累積／降低解除效果；同一需求不會同時被兩者作用。
- 新增妊娠 `expansion`（擴容）症狀：命中的需求上限從 150 提高至 200，`flux` 對應極性也可擴至 ±200；三種症狀不會同時作用於同一需求。
- 擴容需求的前端填色維持 0 至 100 為一般區段，並將警戒色區段由原本的 100 至 150 延伸為 100 至 200；需求等級判定亦隨擴容容量放寬。
- 調整衍生類型的代謝抵免需求，並支援 `fluxPositive` / `fluxNegative` 的阻塞、快積與擴容症狀。
- 更新需求顯示圖標：泄意改用廁紙、伴意改用對話泡泡、快積改用閃電、擴容改用氣球標記，並移除已不用的尿意與便意圖標。
- 代謝互動新增生活循環：進食會帶來泄意與少量困意，睡眠後會產生少量餓意；排泄與泌乳型乳意的解除會帶來臭意，臭意過高會削弱陪伴的舒緩效果。
- 乳意在普通週期表示乳房脹敏或週期不適，於黃體期／月經期隨時間累積，排卵期也可因性慾波動少量累積；妊娠、假孕與產後恢復時則包含泌乳需求。
- 新增跨週生活結算：每滿一週會以基本清潔清除臭意、以日常往來降低部分伴意；普通週期進入新一輪卵泡期時會清除週期型乳意。
- 快積現在也會使剛被緩解的對應需求較快回升，與阻塞形成不同的互動反應。
- `bsUpdatePsychology` 與 `bsMaternalFetalInteraction` 加入每角色、每新小時一次的成功變動冷卻，母胎親和與產兆前驅抵抗統一使用同一個互動冷卻，避免模型重複呼叫造成數值或產程變化過快；解鎖依累積時間跨過完整一小時計算，例如推進 `30 + 30` 分鐘也會解除冷卻。
- 妊娠不適的安撫回補改為累積 `symptomReliefPending` 次數，每次不適都會保留一次待補機會；`direction=maternal` 表示母體安撫胎兒，不傳 `change`、由系統隨機決定 affinity 變化，成功的小幅變化補回 `1` 點供養力、顯著變化補回 `2` 點；`direction=fetal` 則只按 `change` 調整 affinity，不混入供養回補。
- `symptomReliefPending` 會同步提供給主敘事上下文，使一次性提示被後續事件覆蓋後，模型仍能理解尚未安撫完的妊娠不適。
- 系統頁新增「清空所有聊天的追蹤狀態」按鈕，可在變量結構大改後一次移除所有舊聊天的角色狀態、快照、日記與累積時間，並保留插件設定與種族參數。

### 修正

- 修正需求轉換改用實際已緩解量計算，避免需求不足時仍產生過量連鎖累積。
- 修正套用 ST 預設進行異步註冊／追蹤時，內部 payload 會被暫時塞入目前聊天，可能遭主流變量流程捕獲並顯示於角色卡內容的問題。
- 修正主流模式依賴特定預設 prompt 文案黑名單篩選上下文的問題；現在僅採用 ST 實際解析出的已啟用世界書，並在無解析結果時回退至插件自身的條目篩選流程。

## v0.5.0

這版重整了待產到分娩的流程，並補足產程表現與調試工具。

### 新增與調整

- 將短暫的「产前阵痛」調整為可延續數日的「产兆前驱」；時間耗盡後才進入第一產程，抵抗分娩則可延長或縮短前驅時間，成功延產後返回孕期。
- 將胎位校正移至產兆前驅；新增「真实产程」開關，使宮縮加速不會直接跳過產程階段，並可在阻塞自然娩出時提示手術產。
- 細分產程階段：第一產程包含潛伏期、活躍期、過渡期；第二產程包含逐胎下降、娩出與間歇期；第三產程包含供養器官娩出與產後觀察。
- 新增分娩疼痛表現，依產程、分娩難度、活力與情壓變化；前端改以階段卡光暈提示痛感。
- 自然產經驗現在會縮短後續第一產程，並同步反映在前端產程進度顯示。
- 調試頁新增產兆前驅進度控制與胎兒自主活動事件，可將自訂胎動敘事追加至 `notify.secondly`。
- `产后观察` 時數改由 `stage_config.js` 統一設定；產後恢復不再向 LLM 傳送分娩疼痛資訊。
- 一般人類的標準妊娠不再保存無意義的 `x1` 妊娠變速效果。

## v0.4.1

此版為純 Debug 修正。

### 修正

- 修正樓層快照不匹配，導致變量消失的問題。
- 修正妊娠不適流程中 `pregnant` 變量未宣告，導致 `bsTimepass` 報錯的問題。
- 修正角色處於產程或逾期時，前端妊娠欄位消失的問題。
- 修正非角色原生世界書無法勾選排除欄位的問題。
- 現在也能勾選全域世界書條目。

## v0.4.0

這版主要強化妊娠與代謝追蹤，也順手修了幾個使用上的問題。

### 新增與調整

- 新增 `nutrition`，用於支援胎兒體重的動態成長。
- 新增 `blockage`，可讓部分代謝需求被阻塞，用來模擬妊娠症狀。
- `metabolism` 新增 `milk`（奶意）與 `odor`（臭意，表示清潔）兩項追蹤。
- 改善衍生型角色的代謝顯示與描述邏輯。
- 新增代謝需求用 SVG icon，尿意、便意、飢餓、睡眠、奶意、臭意與 flux 都有更直觀的圖示。

### 修正

- 修正 snapshot 相關問題，降低手機上容易 OOM 的情況。
- 修正完整變量頁流程：現在會先進入人物選單，點選角色後才顯示變量內容與調試工具。

## v0.3.0

### 新增與調整

- 新增日記系統：角色註冊時會產生第一篇日記，過了一個故事日後，LLM 能為角色撰寫新的一則內容。
- 追蹤頁調整：妊娠的繁育資料（卵子、妊娠天數、羊膜、分娩時間）隱藏到其他 UI 顯示。

### Debug / 修正

- 修正前端左上角故事日顯示中，天數不會累進的問題。
- 修正切換階段時從第 0 天開始，而不是第 1 天的問題。
- 修正受精後立即懷孕，沒有維持等待著床的問題。
- 修正異種角色的月經長度依然使用人類週期的問題。
- 修正註冊孕婦時，懷孕天數會填入 `effectivePregnantDays` 的問題。
- 修正 tracker 預設不會記憶上次使用，會退回裸預設的問題。

## v0.2.3

### 新增與調整

- 新增世界書排除新規則：主流 API 發什麼，tracker API 就同樣發一次。
- 新增 tracker 預設管理器，現在和主流預設分開設定，更好搭配不同的模型。

## v0.2.2

### 新增與調整

- 讓樓層 snapshot 再壓縮，並讓 fork 繼承條件放寬。
- 讓彈出視窗與主題顏色一致。
- 增加主題頁，可以調整主題和字體大小，並可切換手機 / 平板配置。
- 增加兩種氛圍主題，現為十二種。

## v0.2.1

### 新增與調整

- 每次新對話都會 reload 世界書條目。
- 現在可在變量 page 手動修改變量。
- 增加四種氛圍主題，現為十種。

### 修正

- 修復受精失敗會吃掉卵子的問題。

## v0.2.0

### 新增與調整

- 新增「妊娠變速」機制並兼顧敘事。支持妊娠加速、減速、凍結、超長孕期等設定，並區分 `pregnantDays` 與 `effectivePregnantDays`。
- 新增「套用 ST 當前預設」。異步註冊與追蹤現在可盡量沿用 SillyTavern 當前預設，不只讀取採樣參數，也會跟隨目前啟用的 prompt blocks，提升破限表現。
- 新增世界書「排除 / 參考」模式。可在 WORLDBOOK 頁用單一文本框管理過濾名單，並支持僅參考指定條目，即使條目處於 disabled 也可納入。
- 百科頁新增獨立的衍生 / Flux 查閱區塊，作為純查閱用途。

### 修正

- 修復註冊時世界書整包參考的問題。角色註冊現在也會遵守世界書過濾規則，不再無條件整包傳入 worldbook。
- 修復 `after_ai` 在流式輸出未完成時提前發送的問題。現在會等待 assistant 最後一則消息穩定一段時間後再觸發，避免在流式生成中途誤送。

## v0.1.2

### 優化

- 優化宮壓流產與產程警告邏輯。

### 修正

- 修正新對話建立時殘留舊變量狀態的問題。
- 修正懸浮球 pager 切換時 view 不再退回首頁。

## v0.1.1

### 新增與調整

- 首頁按鈕改成 SVG icon。
- 追蹤更新不再跳系統 log，改成懸浮球發光提示。
- 精液 / 胎兒 / 孩子改成箭頭切換。

### 修正

- 修掉高樓層內存膨脹。
- 修掉聊天 fork 會繼承變量。
- 修掉懸浮球點擊未正常展開。

## v0.1.0

首個公開版本。

本插件為 SillyTavern 擴充，提供角色生理狀態與時間演進追蹤系統，適用於 AIRP / 長期角色扮演場景。
